// tuple is like an array with type for each index

const user:[string,number,boolean]= ["Samir",123,true]// tubles === data type in proper index

const rgb:[number,number,number]= [255,255,255]// only three values can be inserted

type User=[number,number]// only two numbers
const newUser:User=[1,2]
newUser[1]=2// can change